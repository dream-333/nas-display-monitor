#!/bin/sh
set -e
if [ -d /run/systemd/system ]; then
    systemctl daemon-reload
fi
# Preserve the service account and /var/lib state even on purge: pairing and admin
# credentials are user data. Manual deletion is documented, never implicit.
